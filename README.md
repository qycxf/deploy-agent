# deploy-agent

基础项目脚手架。

快速开始：

运行开发服务器：
```bash
go run ./cmd/server
```

生成（占位）：
```bash
make gen
```

构建二进制：
```bash
make build
```
# Go Project

这是一个示例 Go 项目，包含一个简单的 HTTP 服务。

运行：

```
cd deploy-agent
go run .
```

构建：

```
go build -o bin/app
```
